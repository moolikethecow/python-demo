{
  "name": "jest-button-test",
  "version": "1.0.0",
  "description": "CircleCI Demo",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "jest-junit": "^13.0.0"
  },
  "dependencies": {
    "jsdom": "^18.1.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "reporters": [ "default", "jest-junit" ]
  },
  "jest-junit": {
    "outputDirectory": "./test-results/jest",
    "outputName": "results.xml",
    "addFileAttribute": "true"
  }
}
